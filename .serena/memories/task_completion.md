# Task Completion
- Run `npm run build`; it is the project’s type-check and production-build gate.
- For visible UI changes, run `npm run dev` or `npm run preview` and manually inspect the affected page state.
- No repository lint, formatter, or automated test commands are configured.